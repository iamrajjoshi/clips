{
  "name": "@clip/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "rm -rf .astro node_modules/.astro dist",
    "build": "astro build",
    "check": "astro check",
    "lint": "oxlint src && eslint .",
    "test": "tsx --test src/test/*.test.ts"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "cliplink-schema": "^0.1.0",
    "@sentry/astro": "^10.70.0",
    "astro": "^5.18.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8"
  }
}

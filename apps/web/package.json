{
  "name": "@clip/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "rm -rf .astro node_modules/.astro dist",
    "build": "astro build",
    "check": "astro check",
    "lint": "oxlint src && eslint .",
    "test": "tsx --test src/test/clips.test.ts"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "cliplink-schema": "^0.1.2",
    "@sentry/astro": "^10.75.0",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8"
  }
}
